VAULTLINE HANDOFF FOR NEXT BOT
Generated: 2026-05-10

PROJECT GOAL
Build and finish a mobile-first web site named Vault'd, inspired by unlockt.me, where creators sell locked content behind a paywall and fans buy permanent unlocks. This is a mobile web site for now, not a native app.

USER DESIGN DIRECTION
- Keep the UI custom, polished, smooth, and studio-quality. The user rejected anything that looks like a beginner/demo design.
- Mobile-first layout is the priority.
- Unlocks are permanent. Do not add access-time/expiration controls for buyers.
- Do not add categories.
- Do not add post time.
- Price minimum is $5. Show the inline message "minimum of $5" when price is less than $5.
- Do not allow Generate Link/Create when required fields are blank.
- Upload/Sell tab should feel simple like the reference screenshots:
  - Add media first.
  - Price input should be a centered pill like Unlockt, not a plain input or huge cropped text.
  - The main Sell screen should be compact enough to feel like one mobile page with minimal/no scrolling.
  - Allow multiple media files in one drop.
  - Allow mixed photos and videos.
  - Once media is selected, hide the large "Photo, Video" intro block.
  - Show selected media side by side in a draggable/swipeable rail.
  - Each selected media tile needs an X in the top-right corner to remove it.
  - Add more media should remain available.
  - Remove quick preset price buttons ($10/$20/$30).
  - Remove top "Create" text.
- Download option needs a nested extra menu:
  - Allowed
  - Allowed (Charge Extra)
  - Not Allowed
  - If Charge Extra is selected, allow choosing extra percent.
- Profile:
  - Cover image has a fixed set height and can be changed with a Change cover control. No separate plus icon on cover.
  - Profile photo camera icon belongs in the corner of the avatar.
  - Username should not overpower the profile photo.
  - Remove unnecessary "PUBLIC PAGE" label from profile header.
  - Top account chip should open a small dropdown/popover with logout, not a large account modal.
  - Profile settings modal should include account/email settings.
- Wallet:
  - Withdraw to bank button should be green.
- Fan side:
  - Needs the same high-level polish as creator side.
  - Fan/store stats should not look like boxed spreadsheet cells.
- Fees:
  - Creator-side platform commission is 10%.
  - Buyer checkout/invoices should include a separate 15% line labeled "Privacy & security fees".

WHAT IS IN THIS FOLDER
- index.html, styles.css, app.js
  Creator-side mobile prototype.
- fan.html, fan.css, fan.js
  Fan-side mobile prototype.
- assets/
  SVG placeholder assets used by the prototype.
- server/
  Express backend scaffold for auth, creators, drops, uploads, checkout, webhooks, and entitlements.
- db/schema.sql
  Supabase/Postgres schema for users, creator profiles, fan profiles, drops, media, purchases, entitlements, operations, reports, support tickets, audit logs, and storage bucket setup.
- docs/
  Launch notes, Stripe setup, security/compliance notes, and API routes.
- package.json and package-lock.json
  Node dependencies and scripts.
- .env.example
  Template for environment variables. Do not commit or share a real .env with secrets.

WHAT HAS BEEN DONE
- Mobile creator UI has been heavily iterated from the user's feedback.
- Mobile fan UI exists but still needs visual polish.
- Backend scaffold has been added with:
  - Express server
  - Supabase service client integration
  - Stripe checkout/connect/webhook helpers
  - Resend email helper
  - Secure token utilities
  - Storage signed upload/download helpers
  - Auth/session endpoints
  - Drop, purchase, entitlement, wallet, support, and report endpoints
- Supabase project was created in the user's logged-in Supabase account:
  - Project name: vaultd
  - Project ref: srlkkgiwdonjqwjtovog
  - Supabase URL: https://srlkkgiwdonjqwjtovog.supabase.co
  - Region: East US (Ohio), us-east-2
  - Compute: nano
- The Supabase SQL editor automation was unreliable with long pasted schema. The schema should be run manually or via a proper Supabase CLI/API session.
- npm install completed successfully.
- npm run check completed successfully before this handoff.
- Latest update tightened the Sell screen price UI and added the separate Stripe buyer fee line while keeping creator commission at 10%.
- Latest profile update made the launch checklist collapsible and replaced the completed-item green circle with a cleaner check badge.
- Latest settings update made FAQ open inside the Profile settings modal instead of navigating back to the landing page.
- Latest link update makes generated storefront, fan discover, and paid drop links use https://vaultd.me instead of localhost during local testing.
- Latest sell-flow fix makes Generate Link work in local preview mode without calling protected backend upload routes, and keeps selected media File objects available for real uploads.
- Latest fan-discover update removed the redundant Refresh feed button; users can refresh the web page instead.
- Latest fan navigation update makes the Vault'd brand/icon on the logged-out fan page return to the public landing page instead of acting like a fan tab switch.
- Latest Stripe copy update clarifies creators connect their own Stripe payout account and that bank/identity details stay with Stripe.
- Latest launch-readiness update completed items 5, 6, and 7 from the checklist:
  - Preview upload mode is local-only and stripped in production.
  - Public policy center was added at /policies.html.
  - Private admin console was added at /admin.html for launch readiness, reports, and support tickets.
- Latest launch-hardening update completed items 6, 7, and 8:
  - Creator email sign-in and fan phone sign-up now require 18+ confirmation.
  - Backend stores age_confirmed_at and blocks publishing, Stripe onboarding, and checkout for accounts that have not confirmed.
  - Suspended users are blocked from authenticated actions and hidden from public discovery/storefront/drop reads.
  - Admin console now has moderation actions to hide/restore reported drops, suspend/unsuspend creators, revoke purchase entitlements, and view audit logs.
  - API routes now have basic rate limits for auth, writes, checkout, support/report, and admin surfaces.

IMPORTANT SECURITY NOTE
Do not ask the user to paste API keys into chat. If keys are needed, have the user add them directly into a local .env file or deployment secret manager. Do not include .env in this handoff package.

WHAT IS LEFT TO LAUNCH
1. Apply the database schema.
   - Open Supabase project srlkkgiwdonjqwjtovog.
   - Run db/schema.sql in SQL editor, or use Supabase CLI with the project linked.
   - Confirm the private storage bucket "locked-media" exists.
   - If the tables already exist, rerun the full schema or at least add users.age_confirmed_at, users.suspended_at, and users.suspension_reason.
2. Create local/deployment environment variables from .env.example.
   Required:
   - SUPABASE_URL=https://srlkkgiwdonjqwjtovog.supabase.co
   - SUPABASE_ANON_KEY
   - SUPABASE_SERVICE_ROLE_KEY
   - JWT_SECRET
   - STRIPE_SECRET_KEY
   - STRIPE_WEBHOOK_SECRET
   - RESEND_API_KEY
   - EMAIL_FROM
   - PUBLIC_BASE_URL
3. Finish Stripe setup.
   - Configure Stripe Connect for creator payouts.
   - Add webhook endpoint: /api/webhooks/stripe
   - Enable events needed for checkout completion, refunds, disputes, account updates, and payouts.
4. Finish email setup.
   - Verify a sending domain in Resend.
   - Update EMAIL_FROM.
   - Test verification email flow.
5. Test full creator and fan API flows end-to-end on the deployed host.
   - Creator email sign-in with age confirmation.
   - Stripe Connect onboarding.
   - Media upload through signed URLs.
   - Generate paid drop.
   - Fan phone sign-up with age confirmation.
   - Buy/unlock checkout.
   - Fan library permanent access.
   - Signed download/view links only after entitlement check.
6. Test admin and safety workflows on production test data.
   - Add your admin email to ADMIN_EMAILS.
   - Create a test report and hide/restore the reported drop.
   - Suspend/unsuspend a test creator.
   - Revoke a test purchase entitlement and confirm the fan cannot download it.
   - Review /admin.html audit logs.
7. Add production hardening outside the app.
   - Legal review of the new policy center and final business/entity details.
   - DMCA designated-agent registration if pursuing DMCA safe-harbor coverage.
   - Edge/WAF rate limits in the host or CDN in addition to the server's in-memory limits.
   - CORS/domain settings.
   - Deployment setup and smoke tests.

USEFUL COMMANDS
Install dependencies:
  npm install

Run checks:
  npm run check

Run local backend:
  npm run dev

Health check:
  http://localhost:8787/api/health

FILES TO IGNORE
- node_modules/
- .env
- Any real secret keys or downloaded credentials.
